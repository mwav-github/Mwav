<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="board">
	
	<!--/*========================================등록========================================*/  -->
	<insert id="insertBnsForm" parameterType="hashmap">
		<![CDATA[
			INSERT INTO BoardNews_tbl
				SELECT IFNULL(max(bNews_id)+1,100001), #{bnGroup}, #{bnHighLight}, #{bnTitle}, #{bnSubTitle}, #{bnRelatedLink}, #{bnContent}, #{bnViewCount}, #{bnOrder}, #{bnStatus}, #{staff_id}, NOW(), NOW(), null FROM BoardNews_tbl
		]]>
	</insert>

    <!--/*========================================보기========================================*/  -->

   <select id="selectOneBnsView" parameterType="hashmap" resultType="hashmap">
	<![CDATA[
	    select * from BoardNews_tbl where bNews_id = #{bNews_id}
	]]>
	</select>
	
	<!--/*========================================수정========================================*/  -->
	
	<update id="updateHitCnt" parameterType="hashmap">
    <![CDATA[
       update BoardNews_tbl set bnViewCount = iFnull(bnViewCount, 0) +1 where bNews_id = #{bNews_id} 
    ]]>
    </update>
    
    <update id="updateBnsform" parameterType="hashmap">
    <![CDATA[
        update BoardNews_tbl set bnGroup= #{bnGroup}, bnHighLight= #{bnHighLight}, bnTitle= #{bnTitle}, bnSubTitle= #{bnSubTitle}, bnRelatedLink= #{bnRelatedLink}, bnContent= #{bnContent}, bnViewCount= #{bnViewCount}, bnOrder= #{bnOrder}, bnStatus= #{bnStatus}, staff_id= #{staff_id}, bnInsertDt= #{bnInsertDt}, bnUpdateDt= NOW(), bnDeleteDt=null where bNews_id = #{bNews_id}
    ]]>
    </update>
 
	
	<!--/*========================================리스트(SelctOne, SelectList 순)========================================*/  -->
	
	<select id="selectOneGetTotalCount" resultType="int">
	<![CDATA[
	   select count(*) from BoardNews_tbl
	]]>
	</select>
	
	<select id="selectListBnsFrontList" parameterType="hashmap" resultType="hashmap">
		<![CDATA[
			SELECT bNews_id, bnTitle, date_format(bnUpdateDt,'%Y%m%d') as bnUpdateDt
		    FROM BoardNews_tbl
		    WHERE bnDeleteDt IS NULL ORDER BY bnUpdateDt DESC Limit 5
		]]>
	</select>

	
	<!-- .
	normal : select bNews_id, bnGroup, bnTitle, bnSubTitle, bnSubTitle, date_format(bnUpdateDt,'%d %b %Y %T') as bnUpdateDt from BoardNews_tbl WHERE bnDeleteDt IS NULL ORDER BY bNews_id;
	mysql : select c.* from (select @ROWNUM := @ROWNUM + 1 AS ROWNUM, a.* from (select * from BoardNews_tbl order by bnInsertDt desc) a,  (SELECT @ROWNUM := 0) R ) c where c.ROWNUM >= 0 and c.ROWNUM <= 10
    oracle : select * from (select rownum rnum, news_id, bnGroup, bnHighLight, bnTitle, bnSubTitle, bnRelatedLink, bnContent, bnViewCount, bnOrder, bnStatus, staff_id, bnWriteDt, bnModifyDt, bnDeleteDt from (select * from BoardNews_tbl order by bnWriteDt desc)) where rnum >= ? and rnum <= ?	
	-->
	
	<select id="selectListBnsList" parameterType="hashmap" resultType="hashmap">
	<![CDATA[
	select c.* from (select @ROWNUM := @ROWNUM + 1 AS ROWNUM, a.* from (select * from BoardNews_tbl order by bnInsertDt desc) a,  (SELECT @ROWNUM := 0) R ) c where c.ROWNUM >= #{startRow} and c.ROWNUM <= #{endRow}
	]]>
	</select>
	
	
	<!--/*========================================삭제========================================*/  -->
	
	
	<update id="deleteBnsDelete" parameterType="hashmap">
    <![CDATA[
        update BoardNews_tbl set bnDeleteDt = NOW() where bNews_id = #{bNews_id}
    ]]>
    </update>


	<!-- =================================Notice부분========================================== -->
	
	<!--/*========================================등록========================================*/  -->
	<insert id="insertBuForm" parameterType="hashmap">
		<![CDATA[
			INSERT INTO BoardNotices_tbl
				SELECT IFNULL(max(bUsers_id)+1,100001), #{buGroup}, #{buHighLight}, #{buTitle}, #{buSubTitle}, #{buRelatedLink}, #{buContent}, #{buViewCount}, #{buOrder}, #{buStatus}, #{staff_id}, NOW(), NOW(), null FROM BoardNotices_tbl
		]]>
	</insert>
    
    <!--/*========================================보기========================================*/  -->

   <select id="selectOneBuView" parameterType="hashmap" resultType="hashmap">
	<![CDATA[
	    select * from BoardNotices_tbl where bUsers_id = #{bUsers_id}
	]]>
	</select>
	
	<!--/*========================================수정========================================*/  -->
	
	<update id="updateNoticeHitCnt" parameterType="hashmap">
    <![CDATA[
       update BoardNotices_tbl set buViewCount = iFnull(BuViewCount, 0) +1 where bUsers_id = #{bUsers_id} 
    ]]>
    </update>
    
    <update id="updateBuform" parameterType="hashmap">
    <![CDATA[
        update BoardNotices_tbl set bnGroup= #{bnGroup}, bnHighLight= #{bnHighLight}, bnTitle= #{bnTitle}, bnSubTitle= #{bnSubTitle}, bnRelatedLink= #{bnRelatedLink}, bnContent= #{bnContent}, bnViewCount= #{bnViewCount}, bnOrder= #{bnOrder}, bnStatus= #{bnStatus}, staff_id= #{staff_id}, bnInsertDt= #{bnInsertDt}, bnUpdateDt= NOW(), bnDeleteDt=null where bUsers_id = #{bUsers_id}
    ]]>
    </update>
 
	
	<!--/*========================================리스트(SelctOne, SelectList 순)========================================*/  -->
	
	<select id="selectNoticeOneGetTotalCount" resultType="int">
	<![CDATA[
	   select count(*) from BoardNotices_tbl
	]]>
	</select>
	
	<select id="selectListBuFrontList" parameterType="hashmap" resultType="hashmap">
		<![CDATA[
			SELECT bUsers_id, buTitle, date_format(buUpdateDt,'%Y%m%d') as buUpdateDt
		    FROM BoardNotices_tbl
		    WHERE buDeleteDt IS NULL ORDER BY buUpdateDt DESC Limit 5
		]]>
	</select>
    
    <select id="selectListBuList" parameterType="hashmap" resultType="hashmap">
	<![CDATA[
	select c.* from (select @ROWNUM := @ROWNUM + 1 AS ROWNUM, a.* from (select * from BoardNotices_tbl order by buInsertDt desc) a,  (SELECT @ROWNUM := 0) R ) c where c.ROWNUM >= #{startRow} and c.ROWNUM <= #{endRow}
	]]>
	</select>
	
	
	<!--/*========================================삭제========================================*/  -->
	
	
	<update id="deleteBuDelete" parameterType="hashmap">
    <![CDATA[
        update BoardNotices_tbl set buDeleteDt = NOW() where bUsers_id = #{bUsers_id}
    ]]>
    </update>

    

</mapper>