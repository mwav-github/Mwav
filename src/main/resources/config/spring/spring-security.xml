<?xml version="1.0" encoding="UTF-8"?>
<beans:beans xmlns="http://www.springframework.org/schema/security" 
    xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" 
    xmlns:beans="http://www.springframework.org/schema/beans" 
    xmlns:oauth="http://www.springframework.org/schema/security/oauth2"
    xsi:schemaLocation="
    	http://www.springframework.org/schema/security/oauth2 http://www.springframework.org/schema/security/spring-security-oauth2.xsd
    	http://www.springframework.org/schema/security http://www.springframework.org/schema/security/spring-security-3.1.xsd
        http://www.springframework.org/schema/beans  http://www.springframework.org/schema/beans/spring-beans-3.1.xsd">
        
    <beans:bean class="org.springframework.beans.factory.config.PropertyPlaceholderConfigurer">
        <beans:property name="ignoreResourceNotFound" value="true" />
        <beans:property name="ignoreUnresolvablePlaceholders" value="true"/>
    	<beans:property name="locations">
			<beans:list>
 				<!-- <beans:value>classpath:security.properties</beans:value> -->
			</beans:list>
		</beans:property>
    </beans:bean>
       
	<http auto-config="true" use-expressions="true">
		<intercept-url pattern="/authentication" access="permitAll"/>
		<intercept-url pattern="/auth/**" access="permitAll"/> <!-- 인증관련 요소도 기능 오픈 -->
		<intercept-url pattern="/error/**" access="permitAll"/>
		<intercept-url pattern="/login" access="permitAll"/>
		<intercept-url pattern="/accessDenied" access="permitAll"/>
		<intercept-url pattern="/resources/**" access="permitAll"/>
		<form-login login-page="/login.do" default-target-url="/" always-use-default-target="true"/>
		<logout logout-success-url="/login.do" logout-url="/logout.do"/>
	
		
		<session-management><!-- 동시세션 제어 -->
            <concurrency-control max-sessions="1" expired-url="/login?error=expired"/> <!-- 단일 사용자가 동시에 가질수 있는 세션 수와 세션 만료시 이동할 URL -->
        </session-management>
	</http>
	
	<authentication-manager alias="authenticationManager" />
	
	
</beans:beans>

